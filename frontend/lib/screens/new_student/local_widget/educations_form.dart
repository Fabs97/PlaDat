import 'package:flutter/material.dart';

class EducationsForm extends StatelessWidget {
  const EducationsForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}