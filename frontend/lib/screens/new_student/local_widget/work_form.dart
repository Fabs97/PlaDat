import 'package:flutter/material.dart';

class WorkExperiencesForm extends StatelessWidget {
  const WorkExperiencesForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}